= form_for @blog do |f|
  .write_form
    = f.label :title, style: { 'margin-right' => 20 }
    = f.text_field :title, placeholder: 'ブログタイトルを書いてね'
    = f.text_area :text, placeholder: '本文を書いてね！', style: 'width: 100%; height: 200px;'
    = f.submit